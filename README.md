# mackerel-low-usage-police

## Usage

```
Usage:
  mackerel-low-usage-police [OPTIONS]

Application Options:
  -o, --org=
  -s, --service=
  -r, --roles=
  -f, --filter=
      --timewindow= Specify time window (unit: day) (default: 1)
      --prefix=
      --metrics=
      --version     print version and exit
      --debug       Enable debug mode
      --verbose     Enable debug mode

Help Options:
  -h, --help        Show this help message
```
